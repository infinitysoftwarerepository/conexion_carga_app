<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================
         CRÉDITOS / DEVELOPERS
         Proyecto: Conexión Carga
         Empresa: Infinity Software
         Desarrolladores: Daniel David Gaviria Zapata, Danilo Ramirez Osorio
         Año: 2025
         Nota: Comentario informativo (no afecta el build).
         ============================================================ -->

    <!-- ✅ CLAVE: La app usa HTTP/WebView/API => necesita Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- (Opcional) Si en algún punto necesitas detectar conectividad
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    -->

    <application
        android:label="Conexión Carga"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Theme mientras Flutter arranca -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- No borrar: Flutter embedding v2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- ✅ ANDROID 11+ (Package visibility) -->
    <queries>

        <!-- ✅ Requerido por Flutter engine para “Process Text” -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- ✅ (Opcional) Si abres whatsapp:// con url_launcher -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>

        <!-- ✅ (Opcional) Compartir texto (si validas disponibilidad antes de enviar) -->
        <intent>
            <action android:name="android.intent.action.SEND"/>
            <data android:mimeType="text/plain"/>
        </intent>

    </queries>

</manifest>
